<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.spms.mapper.ProjectMapper">

    <insert id="addCostPeople">
        insert into pro_cost_people(pro_id, peo_id, days) values(#{proId}, #{peoId}, #{days})
    </insert>
    <insert id="addCostDevice">
        insert into pro_cost_device(pro_id, dev_id, days) values(#{proId}, #{devId}, #{days})
    </insert>
</mapper>